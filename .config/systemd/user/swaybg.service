[Unit]
PartOf=graphical-session.target
After=graphical-session.target
Requisite=graphical-session.target

[Service]
EnvironmentFile=%h/.config/swaybg/env
ExecStart=/usr/bin/swaybg -m fill -i "${IMG_PATH}"
Restart=on-failure
